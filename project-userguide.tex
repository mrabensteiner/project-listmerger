%----------------------------------------------------------------
%
%  File    :  project-userguide.tex
%
%  Author  :  Martin Rabensteiner
% 
%  Created :  20 Feb 2026
% 
%  Changed :  12 Mar 2026
% 
%----------------------------------------------------------------

\chapter{User Guide}
\label{chap:userguide}

\TODO{}


\section{End Users}

End users can switch between separated list via tabs, or on more narrow
screens with a select box. Items can be dragged and dropped from one
list on the right to the mergelist on the left. An item is inserted at
the location where it is dropped. The \uiname{Move All} button can be
used to move all existing items of a list to the merged list.

By dragging one item over another in the mergelist, those items can be
merged. With the drag handle on the right, they can be rearranged.
\uiname{Undo} and \uiname{Redo} let the user jump through the history.
The behaviour of the \uiname{Save} button is given by the individual
implementation.


\section{Implementors}
This section addresses to those that want to include the listmerger in a
larger software project. To initialise a page with lists, the
\fname{init.init()} function needs to be called. The parameters contain
an overall HTML id, the ids for the \uiname{Undo} and \uiname{Redo}
buttons, and a function that can be called when the \uiname{Save} button
is clicked. This function is then called with an array of moved, and
merged items.


\section{Developers}

The code itself is written in pure TypeScript, without using third party
libraries. They are only used in the build and development process.

Recurring terms in the development are \uiname{move} and \uiname{merge}.
Moving describes the process of a transferring an item from A merged
item always consists of two predecessors. They are either taken from the
mergelist (merged or moved), or directly taken from the evaluators list.
The origin is always tracked, independent whether The differentiation
between moved and origin is done, to have a clean and reconstructive
merging history.

When saving, the order of the merged elements is taken from the list,
while the data about merged is stored in an array.

\subsection{Files}

The TypeScript code is modularised into several files, each serving a
different purpose. The files are:
\begin{itemize}
\item \fname{events.ts}: Initialises all event listeners for buttons
and drag events, and contains their functions.
\item \fname{globalvars.ts}: Contains some shared variables used across several files.
\item \fname{history.ts}: Tracks all actions and recovers them when the \uiname{Undo} or \uiname{Redo} button is pressed.
\item \fname{index.ts}: Serves only as the interface between the module and the outer implementation.
\item \fname{init.ts}: Contains the only function callable from the outside.
\item \fname{transfer.ts}: Handles the transfers between the source lists and the merged list.
\item \fname{uihelper.ts}: Contains several helper functions to deal with the DOM.
\end{itemize}

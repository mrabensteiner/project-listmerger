%----------------------------------------------------------------
%
%  File    :  project-listmerger.tex
%
%  Author  :  Martin Rabensteiner
% 
%  Created :  20 Feb 2026
% 
%  Changed :  17 Apr 2026
% 
%----------------------------------------------------------------

\chapter{ListMerger}
\label{chap:listmerger}

\TODO{}

This section describes the implementation of ListMerger. The code itself
is written in pure TypeScript. Almost all third-party librariers are
only used in the build and development process. Only the Mustache
package is used to render templates.

Recurring terms in the development are \uiname{move} and \uiname{merge}.
Moving describes the process of a transferring an item from A merged
item always consists of two predecessors. They are either taken from the
\uiname{mergelist} (merged or moved), or directly taken from the
evaluators list. The origin is always tracked, independent whether The
differentiation between moved and origin is done, to have a clean and
reconstructive merging history. When saving, the order of the merged
elements is taken from the list, while the data about merged is stored
in an array. 


\section{Files}

The TypeScript code is modularised into several files, each serving a
different purpose. The files are:
\begin{itemize}
\item \fname{events.ts}: Initialises all event listeners for buttons and
drag events, and contains their functions.
\item \fname{globalvars.ts}: Contains some shared variables used across
several files.
\item \fname{history.ts}: Tracks all actions and recovers them when the
\uiname{Undo} or \uiname{Redo} button is pressed.
\item \fname{index.ts}: Serves only as the interface between the module
and the outer implementation.
\item \fname{init.ts}: Contains the only function callable from the
outside.
\item \fname{transfer.ts}: Handles the transfers between the source
lists and the merged list.
\item \fname{uihelper.ts}: Contains several helper functions to deal
with the DOM.
\end{itemize}



\section{Data Structure}
When initialising, a JavaScript object has to be handed over to the
library in order to build the application.
% TODO
More details on this initialisation can be found in
Appendix~\ref{chap:libuser}.


\section{History (Undo/Redo)}

An important feature and affordance of this component is the history
tracking. With the \uiname{Undo} and \uiname{Redo} buttons, the user can
jump back and forward with actions.

The history is based on a history and a future array. When clicking
undo/redo, a history item is moved from history to future, or vice
versa. A history item consists of a keyword for the action, and three
parameters. The parameters are strings, and can be IDs, or positions,
depending on the context of the merge keyword.

% tracking history



\section{Initialisation}
A detailed guide to the initialisation of a ListMerger instance can be
found in Appendix~\ref{chap:libuser}.
\TODO{}



\section{Moving Items}
\TODO{}



\section{Styles}
The ListMerger itself does not define any styles. These are entirely up
to the individual implementation. Only the base-structure of the
application is to a certain extent given by the library itself. The demo
files can be used as a staring point or completely 

\section{Accessibility Mode}
WCAG 2.2 Success Criterion 2.5.7 requires alternative methods 
% Drag and Drop Accesbility


To activate this accessibility mode, the
\vname{toggleAccessibilityMode()} function needs to be called. This can
be done when initialising the library with a
\vname{toggleAccessibilityMode(true)} call, to always initalise it in
the beginning. When toggling is afforded, the function without a
parameter can be bound to a button.

The accessibility does not affect sizes, colors, or other visual
properties of the page, since the are entirely depending on the
implementation on the page and the custom CSS. 



% drag and drop

% tracking in lists



\section{Cursor Behaviour when Dragging}
The \fname{event.datatransfer.dropEffect} property in JavaScript defines
the visual behaviour of the mouse cursor during a drag event
\parencite{mdn-dropeffect}. While the \fname{cursor} CSS property offers
a variety of 36 mouse cursors, in interaction with the operating system
\parencite{mdn-cursor}, this property is not taken into account as soon
as a drag event is started. The \fname{dropEffect} has offers a
selection of only four elements, all equivalent to options for the CSS
property.
% bug in firefox




\section{Templates}
To make ListMerger as flexible as possible, some element specifications
are placed in the hands of templates. For this, the Mustache template
engine is used (see Section~\ref{sec:mustache}). The templates used are
for three parts. Those are:
\begin{itemize}
\item \liintro{Item}: The appearance of a single item in the origin
list, or the merged one.

\item \liintro{Dialog}: When set, a dialog opens when clicking on an
item.

\item \liintro{Merge}: The middle column when open in the merge screen.
If the \vname{name} attribute of an Input or TextArea field is ident to
a value of an item, it is buttons are shown to add the value 
\end{itemize}





\section{Tab Bar}

\begin{samepage}
\begin{lstlisting}[%
float=tp,
aboveskip=\floatsep,
belowskip=\floatsep,
xleftmargin=0cm,              % no extra margins for floats
xrightmargin=0cm,             % no extra margins for floats
basicstyle=\footnotesize\ttfamily,
frame=shadowbox,
numbers=left,
language=CSS,
label=list:tabbarcss,
caption={[Tab Bar CSS]%
The minimal CSS styling to use the \elname{details} element and its
children as a tab bar.
},
]
.container {
  display: flex;
  flex-wrap: wrap;
}

details {
  display: contents;
}

details[open]::details-content {
  display: contents;
}

summary {
  order: 0;
}

section {
  order: 1;
  width: 100%;
}
\end{lstlisting}
\end{samepage}


For a long time, it was necessary to use JavaScript event listeners for
tab bars in the web. By adding click event listeners to the title, the
appearance of the content below in that way can be controlled. The
\elname{details} HTML element and some CSS adjustments make it possible
to have tab bars without JavaScript.

The \elname{details} element serves as a container for one item. On
\elname{summary} element is needed as a child, which is represents the
title. If no such title element is present, the total is
\uiname{Details}. Per default, a small triangle on the left indicates
the open/closed status of the content. This status is toggled by
clicking on the \elname{summary} element. The content does not
necessarly have to be an element and can also be plain text. To have a
clear structure, it is still advisable to have an element to give the
content a better structure, for example with a \elname{section} element.
With the \vname{open} attribute, the fold out of \elname{details} can be
controlled. The \vname{name} attribute can be used to only open one
\elname{details} box when having several instances.

The original purpose of this element is to build Accordions. To use it
for tab bars, some CSS properties need to be applied. These properties
include:
\begin{itemize}
\item \liintro{\elname{container} as flexbox}: This ensure to have the
elements next to each in horizontal alignment.

\item \liintro{\elname{section} with full-width (100\%)}: Below the flex
container, the summaries should have only as much width as they need,
but the section below should span across the whole width to break in a
new line.

\item \liintro{Display details as content}: The section would be shown
right next to the summary, but this display option shifts it to the end.

\item \liintro{Display open details pseudo element as content}: This
pseudo element 

\item \liintro{Order of summary and section}: Setting the order for all
summary items to 0 and all section items to 1, clusters the summary
items all at the top and shows the active section element below.
\end{itemize}

The minimal CSS to reach this appearance in shown in
Listing~\ref{list:tabbarcss}. Additionaly to that, the tabs are also
styling with some basic CSS, including, colors, borders,
border-roundings, margins, paddings, and the cursor, as seen in
Figure~\ref{fig:tabs}.

\begin{figure}[tp]
\centering
\includegraphics[frame,keepaspectratio,width=\halfh,height=\thirdh]
{images/tabs-i.png}
\caption[Tabs]{%
The tabs in ListMerger show the title of each list and an indicator with
the number of added/merged and total items in the list.
\imgcredit{Screenshot by Martin Rabensteiner.}
}
\label{fig:tabs}
\end{figure}



% Responsive Tabbar
However, to maintain responsiveness the solution in ListMerger still
uses JavaScript to transform the bar of tabs in a selection box on
smaller screens or with longer titles.  A \elname{select} box holds the
titles as values and the order as values. The value of the box needs to
be tracked and updated as well when the open tab changes. It also needs
to update when elements are called with a hash in the URL.

% observer
A \vname{ResizeObserver} instance helps to track the size or position of
an element, changed for example through scrolling or resizing the
window. In this case it is used to observe the position of the
\elname{summary} elements, representing the titles of the tabs. If the
titles are too long for the viewport, the titles would break and be
represented in more than one line. This is not a suitable solution for
tabs and they should be controlled by a select box, as described before.
Within the observer, the top offset of the first and last object are
compared, and if they are not the same, the responsive solution should
be applied. A CSS class (\cssname{.compact} in the example) is added to
the tab list. The titles can not be simply hidden, as this would give
the observer an undetermined width and lever its functionality.
Therefore, the titles are made transparent, paddings removed, and the
height set to \vname{1px}. This makes it practically invisible, but
leaves the observer functionality intact.



\section{Icons}

Especially the merging icon made it necessary to work out custom icons
for this project. The icons were initally drafted in Inkscape and the
resulting SVG files manually revised and improved. All icons base on a
50 x 50 viewbox. Strokes have usually a width of 12 and a round
linejoin. All icons are listed in Table~\ref{tab:icons}. The iconset
does not have to be used, but can also be replaced or hidden completely.
\begin{table}[tp]
\tablerowstretch
\rowcolors{2}{}{tablerowcolour}
\centering
\begin{footnotesize}
\begin{tabularx}{\linewidth}
{>{\leavevmode\kern-\tabcolsep}m{2cm}lY<{\kern-\tabcolsep}}
\toprule
\textbf{Icon} & \textbf{Filename} & \textbf{Description} \\
\midrule
\includegraphics[height=2cm]{icons/save} & \fname{save.svg} &
Save the current state. \\
% TODO
\bottomrule
\end{tabularx}
\end{footnotesize}
\caption[Icons]%
{Overview of icons made for and used within the library.}
\label{tab:icons}
\end{table}


